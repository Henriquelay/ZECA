// Nested functions are not implemented
fn add (x, y) {
    x + y;
}

fn main() {
    let x = - 5.03;
    // some comments
    let y = -3.0 - x;
    /* some more
    comments */
    let t = true;
    add(x, y);
}
