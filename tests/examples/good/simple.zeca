// Nested functions are not implemented
fn add (x, y) {
    x + y;
}

fn main() {
    let five = 5.03;
    // some comments
    let eight = 3.0 + five;
    /* some more
    comments */
    let t = true;
    add(five, eight);
}
