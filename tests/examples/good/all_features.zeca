// WIP
// Tests all ZECA features (including comments)


/* Returns the absolute value of a f64
 * AKA flip the sign if float is negative
 */
fn absolute_value(x) {
    if (x < 0) {
        x = -x;
    }

    return x;
}

fn add x y = x + y;

let x = -5;
loop {
    if x <= 5 {
        x = add(x, 1)
        x = absolute_value(negative)
    }
    else {
        break;
    }
}
